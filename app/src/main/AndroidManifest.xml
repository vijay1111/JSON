<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vijay.json">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DisplayRecyclerView.DisplayDataActivity" />
        <activity android:name=".UploadImage.UploadImageActivity" />
        <activity android:name=".ViewFlipper.ViewFlipperActivity" />
        <activity android:name=".ExpandableRecyclerView.ExpandableRecyclerViewActivity" />
        <activity android:name=".VolleyAndroidHive.Volley_Receive_Data" />
        <activity android:name=".GSON_With_Volley.GsonWithVolley" />
        <activity android:name=".RetrofitCheezyCode.RetrofitActivity" />
        <activity android:name=".RecyclerView.RecyclerViewActivity" />
        <activity android:name=".Jsoup.JsoupActivity"></activity>
    </application>

</manifest>